##MACOS Setup Script

#Prerequisites
zsh

#Install Brew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

#Install font
brew install font-fira-code-nerd-font

#install ITerm
brew install iterm2
import profiles and settings from iterm folder 

#install essential brew for Terminal

brew tap "homebrew/command-not-found"
brew install coreutils
brew install asdf
brew install bat
brew install duf
brew install eza 
brew install fd
brew install git-delta
brew install gping
brew install grc
brew install gtop
brew install hexyl
brew install htop
brew install httpie
brew install midnight-commander
brew install fastfetch
brew install nmap
brew install procs
brew install ripgrep
brew install stow
brew install tldr
brew install tmux
brew install tree
brew install wget
brew install git
brew install micro
brew install dust
brew install oracle-jdk@21
Brew install node
brew tap mac-cleanup/mac-cleanup-py
brew install mac-cleanup-py
brew install lazygit
brew install lazydocker

htop (neet sudo to correctly display all running process)
nmap (If using `ndiff` returns an error about not being able to import the ndiff module, try:  chmod go-w /usr/local/Cellar)

rm -Rf rm -Rf /usr/local/Cellar/git/2.49.0/share/zsh/site-functions/_git
sudo ln -s /usr/share/zsh/5.9/functions/_git /usr/local/Cellar/git/2.49.0/share/zsh/site-functions/_git
ln -sf /usr/local/Cellar/openssl@1.1/1.1.1o/lib/libcrypto.dylib /usr/local/lib/ (for sublimetext ackage manager)ß

#Install asdf Version 0.16.5 cause an error with completition on set
wget https://raw.githubusercontent.com/Homebrew/homebrew-core/5d2cba2f99bf08eb200848c414b847feb07341e9/Formula/a/asdf.rb 0.16.5
wget https://raw.githubusercontent.com/Homebrew/homebrew-core/989239e6f79936b0572fb0b5d28f49a82b4f8cde/Formula/a/asdf.rb 0.16.4
brew install asdf.rb
asdf plugin add java
asdf plugin add maven
asdf plugin add nodejs

asdf install java oracle-17
asdf install maven latest
asdf install nodejs 22.14.0


asdf_update_java_home() {
  local java_path
  java_path="$(asdf which java)"
  if [[ -n "${java_path}" ]]; then
    export JAVA_HOME
    if [[ "$java_path" == "/usr/bin/java" ]]; then
    	JAVA_HOME="$(/usr/libexec/java_home)"
    else	
    	JAVA_HOME="$(dirname "$(dirname "${java_path:A}")")"
    fi
    export JAVA_HOME=${JAVA_HOME}	
    export JDK_HOME=${JAVA_HOME}
}

autoload -U add-zsh-hook
add-zsh-hook precmd asdf_update_java_home

link all configs
-----------------
git clone https://github.com/jandamm/zgenom.git .zgenom 
git clone https://github.com/unixorn/zsh-quickstart-kit.git .zqs 
git clone https://github.com/michelemadonna/dotfiles.git .dotfiles 
cd $HOME/.zqs && stow --target=$HOME zsh && cd  
cd $HOME/.dotfiles/zsh && stow --target=$HOME zsh && cd  
cd $HOME/.dotfiles/zsh && stow --target=$HOME/.zshrc.d zshrc.d && cd
cd $HOME/.dotfiles/ssh && stow --target=$HOME/.ssh ssh && cd 

#VIM (https://github.com/spf13/spf13-vim)
sh <(curl https://j.mp/spf13-vim3 -L) 

##VS CODE (https://bobbyhadz.com/blog/vscode-export-settings-and-extensions)
use sync setting using github account

Exporting your VS Code settings and extensions
open the Command Palette and Type export profile and click on Profiles: Export Profile....
Importing a Profile in VS Code 
open the Command Palette and import profile and click on Profiles: Import Profile....

#Intellij
use sync setting GitHub account

PATH="/usr/local/opt/coreutils/libexec/gnubin:$PATH"



#SUBLIME TEXT
install package manager
install ayu theme
change font to firacode

git credential-osxkeychain
git config --global credential.helper osxkeychain
ssh-add -K ~/.ssh/id_rsa_whatever
cat ~/.ssh/config
Host *
  AddKeysToAgent yes
  UseKeychain yes

or

Host *
  IgnoreUnknown AddKeysToAgent,UseKeychain
  AddKeysToAgent yes
  UseKeychain yes
  IdentityFile ~/.ssh/id_ed25519

/usr/bin/ssh -T git@github.co


#### Ubuntu 24 Install
default-jdk-headless

ASDF
mkdir -P $HOME/.asdf/bin
cd $HOME/.asdf/bin
wget from release
tar zxv asdf.tgz

installare node di sistema solo dopo aver installato il plugin di nodejs, aver installato una versione di node da asdf e settata come globale
creare un link simbolico per nodejs in .asdf/shim ln -s node nodejs

#FONT
cd
mkdir .fonts
fc-cache -f -v